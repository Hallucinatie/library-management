# 图书馆管理系统后端

这是一个基于 Node.js 和 Express 框架开发的图书馆管理系统后端API，提供图书和论文的管理、借阅等功能。

## 快速开始

1. 安装依赖
    ```bash
    npm install
    ```

2. 启动服务器
    ```bash
    npm run dev
    ```
3. 初始化数据库(可选)
    ```bash
    sh init_database.sh
    ```
    **注意**：已经初始化数据库。如果需要重新初始化数据库，请通知团队成员。